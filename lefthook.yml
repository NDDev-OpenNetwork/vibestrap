pre-commit:
  parallel: true
  jobs:
    - name: frontend
      root: frontend/
      run: bun run check
      glob: "frontend/**/*.{js,jsx,ts,tsx,json,jsonc,css}"
    - name: backend
      root: backend/
      run: uv run ruff check . && uv run ruff format --check .
      glob: "backend/**/*.py"
