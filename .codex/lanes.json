{
  "protected_branches": ["main"],
  "lane": "feat/<issue>-<slug> -> <user> -> dev -> main",
  "orchestrator_marker": ".agent/orchestrator",
  "note": "members merge their own lane into dev themselves (pull dev, green, --no-ff, push) — dev is shared; main pushes and PR merges run from the integrator checkout which carries the untracked marker file"
}
