# Next session

Template state: fork surface synced with hack-setup (hooks, lanes,
config). Stack law: `build/stack-pin.json` (fork exceptions inside).
Proof: `bun run verify`. Domain memories land in `.serena/memories/`
as features ship — none yet, this is the template baseline.
